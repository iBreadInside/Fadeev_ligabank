export const getRates = (state) => state.rates;

export const getHistory = (state) => state.history;

export const getDate = (state) => state.date;

export const getIsRatesDataLoaded = (state) => state.isRatesDataLoaded;
